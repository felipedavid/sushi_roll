{{define "title"}}
    Home
{{end}}

{{define "main"}}
    {{with .Games}}
        <ul>
            {{range .}}
                <li>{{.Id}} - {{.Title}} - {{.Description}}</li>
            {{end}}
        </ul>
    {{else}}
        <h2>Nenhum jogo registrado</h2>
    {{end}}
{{end}}